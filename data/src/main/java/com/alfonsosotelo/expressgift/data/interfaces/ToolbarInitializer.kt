package com.alfonsosotelo.expressgift.data.interfaces

/**
 * Implements it in a Fragment
 * Use this interface to set data to the Toolbar in the Activity from the Fragment
 */
interface ToolbarInitializer {
    fun initToolbar()
}